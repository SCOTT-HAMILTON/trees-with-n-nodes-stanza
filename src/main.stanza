defpackage tree/main :
   import core
   import collections
   import tree
   import tree/utils
   import tree/tests
   import utils

defn main () :
    ; test-trees()
    ; test-utils()
    var counter = 0
    for t in trees-with-n-nodes-with-m-leaves(10, 5) do :
        counter = counter + 1
        println("T#%_: %_" % [counter, t])
    println("counter: %_" % [counter])
main()

